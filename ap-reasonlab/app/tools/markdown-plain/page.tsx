import MarkdownPlainTool from "@/components/MarkdownPlainTool";

export const metadata = {
  title: "Markdown plain text — Liji Explore",
};

export default function Page() {
  return <MarkdownPlainTool />;
}
