import MistakeNotebookTool from "@/components/MistakeNotebookTool";

export const metadata = {
  title: "Mistake notebook — NauWiki Explorer",
};

export default function Page() {
  return <MistakeNotebookTool />;
}
