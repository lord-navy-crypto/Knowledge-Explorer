import EncodeDecodeTool from "@/components/EncodeDecodeTool";

export const metadata = {
  title: "Base64 & URL encoder — Knowledge Explorer",
  description: "Encode and decode Base64 and URL strings in your browser.",
};

export default function EncodeDecodePage() {
  return <EncodeDecodeTool />;
}
