import SciNotationTool from "@/components/SciNotationTool";

export const metadata = {
  title: "Scientific notation — Liji Explore",
};

export default function Page() {
  return <SciNotationTool />;
}
