import FormulaBoardTool from "@/components/FormulaBoardTool";

export const metadata = {
  title: "Formula board — NauWiki Explorer",
};

export default function FormulaBoardPage() {
  return <FormulaBoardTool />;
}
