import MarkdownPdfTool from "@/components/MarkdownPdfTool";

export const metadata = {
  title: "Markdown → PDF — NauWiki Explorer",
};

export default function MarkdownPdfPage() {
  return <MarkdownPdfTool />;
}
