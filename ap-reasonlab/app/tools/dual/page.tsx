import DualColumnEditor from "@/components/DualColumnEditor";

export const metadata = {
  title: "Dual-column editor — NauWiki Explorer",
};

export default function DualToolPage() {
  return <DualColumnEditor />;
}
