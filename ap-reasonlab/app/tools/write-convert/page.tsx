import WriteConvertWizardTool from "@/components/WriteConvertWizardTool";

export const metadata = {
  title: "Write & convert wizard — Knowledge Explorer",
  description: "Batch handoff between writing, word count, and PDF export tools.",
};

export default function WriteConvertPage() {
  return <WriteConvertWizardTool />;
}
