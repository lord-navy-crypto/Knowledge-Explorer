import UnitsConstants from "@/components/UnitsConstants";

export const metadata = {
  title: "Units & constants — Liji Explore",
};

export default function UnitsPage() {
  return <UnitsConstants />;
}
