import CsvMarkdownTool from "@/components/CsvMarkdownTool";

export const metadata = {
  title: "CSV → Markdown — Knowledge Explorer",
};

export default function CsvMarkdownPage() {
  return <CsvMarkdownTool />;
}
