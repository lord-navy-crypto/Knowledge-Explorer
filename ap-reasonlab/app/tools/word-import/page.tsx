import WordImportTool from "@/components/WordImportTool";

export const metadata = {
  title: "Word → Markdown — Liji Explore",
};

export default function WordImportPage() {
  return <WordImportTool />;
}
